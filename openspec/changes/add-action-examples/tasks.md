# Tasks: Action Examples

## 1. Implementation

- [ ] 1.1 Add at least one titled, realistic example to every action registration (two for create-work-item and search-work-items), extend the discovery test to assert example coverage, update the README note, and run the verification suite (tsc, lint, tests, prettier, build).
